import {FormatDateDistancePipe} from './format-date-distance.pipe';

describe('FormatDateDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
